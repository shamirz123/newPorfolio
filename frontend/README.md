# Frontend

React + Vite portfolio site with admin UI for managing projects.

## Run

```bash
npm install
npm run dev
```

Opens at `http://localhost:3003`.

Admin login: `http://localhost:3003/admin/login`

Requires the backend API on port `5000` (Vite proxies `/api` and `/uploads`).
